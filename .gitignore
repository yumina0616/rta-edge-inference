.venv/
__pycache__/
*.pyc
.pytest_cache/

# 재생성 가능한 모델 산출물 (model_manifest.md/export_model.py로 재생성)
models/*.pt
models/*.onnx
models/openvino/

# 테스트용 영상(용량 큼, 라이선스 있는 외부 소스) 및 디버그용 미리보기 이미지
data/videos/
first_frame_preview.png

# 파이프라인 실행 결과 (재생성 가능)
results/
